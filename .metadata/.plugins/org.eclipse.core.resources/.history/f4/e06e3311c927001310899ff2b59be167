package ui.PlayerView;

import javax.swing.ImageIcon;
import javax.swing.JLabel;

/**
 * Packt Skillicon, skillwert, und skillupmöglichkeit zusammen
 * @author Katy
 *
 */
public class IconStatElement {
	
	private JLabel iconLabels;
	private JLabel statLabels;
	private JLabel skillPointsLabel;
	
	public IconStatElement(){
		initIconStatElement();
	}

	private void initIconStatElement() {
		iconLabels = new JLabel();
		statLabels = new JLabel();
		skillPointsLabel = new JLabel();
	}
	
	//Weiß nich ob der Typ ImageIcon für uns funktioniert, dann mal testen
	private void setIconLabel(ImageIcon image){
		iconLabels.setText("icon");
	}
	
	private void setstatLabels(int value){
		iconLabels.setText((String)42);
	}

	private void skillPointsLabel(){
	
	}

}
